\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb}
\usepackage{sectsty}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{parskip}
\usepackage[superscript]{cite}

% Section formatting without underline conflicts
\allsectionsfont{\centering \normalfont\scshape}
\sectionfont{\normalfont\scshape}
\subsectionfont{\normalfont\bfseries}

% List formatting
\setlist[itemize]{noitemsep, topsep=0pt}

% Hyperref setup
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=cyan,
}

% For better URL handling
\usepackage{url}
\urlstyle{same}
\def\UrlBreaks{\do\/\do-\do\_}